import { container } from '@/app/styles/container'
import clsx from 'clsx'

type PropsType = {}

export const ProductNameSection = (props: PropsType) => {
	const {} = props
	return <section className={clsx('pt-10', container)}>123</section>
}
